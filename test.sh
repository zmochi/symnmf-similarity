#!/usr/bin/env bash
# this is called a 'shebang' and tells the shell which environment to run the script in when you run it with './test.sh'
# this could be equivalently #!/usr/bin/env python for python scripts

# use 'diff file1 file2' to compare 2 files
# see usage for the C program by doing './symnmf' after executing make, results are printed to stdout
# 'diff file1 <(cmd)' replaces the <() with a file containing the output of command 'cmd'
